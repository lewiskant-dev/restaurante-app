Coloca aquí el SVG real del logo principal de Nexo.

Nombre esperado:

- `nexo-logo.svg`

Ruta completa:

- `public/brand/nexo-logo.svg`

Si el archivo no existe, la app usará automáticamente el fallback vectorial definido en código.
